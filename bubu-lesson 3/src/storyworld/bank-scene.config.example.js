// Copy to bank-scene.config.js (gitignored). No key belongs in this file.
// In the Bubu app the scene calls /api/bubu/nessie-portfolio and
// /api/bubu/nessie-action, and the server holds NESSIE_API_KEY.
window.__NESSIE_BASE__ = '/nessie';
